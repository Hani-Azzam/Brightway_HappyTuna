[pytest]
asyncio_mode = auto
markers =
    smoke: calls every read tool once against the live servers
    writes: creates and patches real tickets/channels (off by default)
addopts = -m "not smoke and not writes"
